'use client'
import React from 'react'
import { MainContainer } from '../../page'

const page = ({ params }: { params: { id: string } }) => {
    return (
        <>
            hi
        </>
    )
}

export default page